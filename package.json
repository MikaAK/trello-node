{
  "name": "trello-node",
  "version": "0.0.1",
  "description": "Trello client for node",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && babel src --out-dir dist",
    "preversion": "npm run build",
    "preversion": "git push origin master && npm publish"
  },
  "keywords": [
    "Trello",
    "node"
  ],
  "author": "Mika Kalathil <me@mikakalathil.ca>",
  "license": "MIT",
  "dependencies": {
    "request": "^2.69.0"
  },
  "devDependencies": {
    "babel-core": "^6.5.1",
    "babel-polyfill": "^6.5.0",
    "babel-preset-es2015": "^6.5.0"
  }
}
